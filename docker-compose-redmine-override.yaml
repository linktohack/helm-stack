services:
  redmine:
    ClusterIP: {}
    NodePort: {}
    LoadBalancer: {}
    Ingress: {}
    Deployment:
      spec:
        template:
          spec:
            containers:
              - name: override-namexxx
                imagePullPolicy: Always
volumes:
  db:
    PV:
      spec:
        capacity:
          storage: 10Gi
        persistentVolumeReclaimPolicy: Retain
    PVC:
      spec:
        resouces:
          requests:
            storage: 10Gi
configs:
  redmine_config:
    ConfigMap:
      data:
        hello.yaml: there
secrets:
  not_tested:
    Secret:
      stringData:
        hello.yaml: there        